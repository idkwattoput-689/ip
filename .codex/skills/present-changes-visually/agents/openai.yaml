interface:
  display_name: "Present Changes Visually"
  short_description: "Create a split-view HTML diff page for this project"
  default_prompt: "Use $present-changes-visually to create a visual diff for this project's current changes."
